import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class Environment here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class Environment extends Actor
{
    /**
     * Act - do whatever the Environment wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public void act() 
    {
        // Add your action code here.
    }
    
    public void addedToWorld(World MainWorld) {
            //Attempt to have objects turn by a random amount when spawned in
            //to add visual variety
            //int turnAmount = ((Greenfoot.getRandomNumber(3)+1))*90;
            //turn(turnAmount);
        }
}
